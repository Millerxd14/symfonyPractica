{% extends 'base.html.twig' %}

{% block title %}
{{ parent() }} - Materias
{% endblock %}
{% block stylesheets %} {{ parent() }} {% endblock %}
{% block body %}
    {{ parent() }}
    <div class="container">
        <div class="row">
            {{form(formulario)}}
        </div>
        <div class="row justify-content-center">
            <a class="btn btn-primary" href="{{path('estudiantes_registro')}}">Registrar nueva materia</a>
        </div>
    </div>
    {% block javascripts %}
    {{ parent() }}
    {% endblock %}
    
    
{% endblock %}